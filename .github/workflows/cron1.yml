name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'  # mỗi 14 phút

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -s https://xsmn.onrender.com/api/results > /dev/null
