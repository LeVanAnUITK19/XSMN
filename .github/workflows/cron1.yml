name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # mỗi 14 phút
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -s https://xsmn.onrender.com/api/results/health > /dev/null
