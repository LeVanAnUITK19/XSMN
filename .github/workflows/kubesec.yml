name: Keep Alive

on:
  schedule:
    - cron: '*/12 * * * *'  # mỗi 12 phút

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -s https://xsmn.onrender.com/api/results/health > /dev/null
